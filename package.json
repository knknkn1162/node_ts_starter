{
  "scripts": {
    "build": "tsc -p .",
    "exec": "node ./dist/index.js",
    "test": "yarn run build && yarn run exec && yarn run clean",
    "watch": "tsc --watch",
    "clean": "rm -r ./dist && mkdir dist",
    "fix": "tslint --fix './src/**/*.ts{,x}'",
    "lint": "tslint --format stylish './src/**/*.ts{,x}'"
  },
  "dependencies": {
    "@types/node": "^10.5.2"
  },
  "name": "mido",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^5.10.0"
  }
}
